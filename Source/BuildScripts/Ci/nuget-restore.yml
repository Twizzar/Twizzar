# packs and publishes the nuget package

steps:
  #- task: Cache@2
  #  displayName: Cache NuGet packages
  #  inputs:
  #    key: 'nuget | "$(Agent.OS)" | $(Build.SourcesDirectory)/**/packages.lock.json, !**/bin/**,!**/obj/**'
  #    restoreKeys: |
  #      nuget | "$(Agent.OS)"
  #      nuget
  #    path: $(NUGET_PACKAGES)

  - task: NuGetCommand@2
    displayName: Restore solution
    inputs:
      restoreSolution: '$(solution)'
      feedRestore: f9f2aaff-9a9b-4756-b92f-15871a1ea672/c8ce65cc-7dad-4bef-adb4-1b582fe30bb2